# Refactor Plan — Phần còn lại

## Đã hoàn thành ✅
- `cogs/__init__.py` ✅
- `pyrightconfig.json` ✅
- Xóa dead code (bar_renderer, stat_image, views/give) ✅
- Chuyển scripts ✅
- `models/tu_si.py` TypedDict ✅
- `data/game_data.py`, `bi_canh_data.py`, `boss_data.py` ✅
- `utils/config.py` đã import từ data/ ✅
- `utils/db_boss.py`, `db_market.py`, `db_social.py` ✅
- `utils/database.py` đã re-export ✅

---

## Còn lại — code trực tiếp, KHÔNG dùng subagent

### Bước 1: Tách `data/cong_phap_data.py`
- File: `cogs/cong_phap.py` → tách data (dòng 16-216) sang `data/cong_phap_data.py`
- Cập nhật `cogs/cong_phap.py`: import từ data/, xóa data inline

### Bước 2: Tạo `cogs/views/_session.py`
- File: `cogs/hoso_utils.py` → tách `BiCanhSession`, `_bc_sessions`, `SESSION_TIMEOUT_SECS`, `_cleanup_stale_sessions` (dòng 78-115) sang `cogs/views/_session.py`
- Cập nhật `hoso_utils.py`, `_common.py`, `hoso.py`: import từ `_session`

### Bước 3: Thêm `from __future__ import annotations`
- Thêm vào TẤT CẢ .py files trong project

### Bước 4: Fix Pylance errors cơ bản
- Fix bare `dict` → `dict[str, Any]` ở key functions
- Fix string-quoted return types `"dict | None"` → `dict | None`
- Xóa aliased local imports (`import json as _jlop2` → top-level)

---

## Verification
```bash
python -m py_compile bot.py
python -m py_compile cogs/cong_phap.py
python -m py_compile cogs/hoso.py
python -m py_compile utils/database.py
```
